:information_source:️ `@rwrs servers <type> <--ranked>`

_{short_description}_

**Parameters:**

- `type`: optional. One of `vanilla`, `pacific` or `rwd`. Defaults to none

**Options:**

- `--ranked`: only return ranked (official) servers