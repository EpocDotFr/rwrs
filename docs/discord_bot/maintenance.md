:information_source:️ `@rwrs maintenance action`

_{short_description}_

The whole RWRS website as well as this bot will be unavailable, except for admins.

**Parameters:**

- `action`: required. One of `enable` or `disable`