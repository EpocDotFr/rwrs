## Data Structures

### Server Map Simple Object

  + `name`: `Railroad Gap` (required) - Map name

### Server Map Full Object (Server Map Simple Object)

  + `preview_image_url`: `https://rwrstats.com/images/maps/preview/vanilla.winter/map4.png` - Map preview image URL

### Server Location Object

  + `name`: `Frankfurt am Main, Germany` (required) - City name (if available) + country name of the server location
  + `country_code`: `de` (required) - [ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code of the server location

### Server Players Simple Object

  + `current`: `20` (number, required) - Number of current players
  + `max`: `24` (number, required) - Maximum allowed number of players
  + `free`: `4` (number, required) - Number of free player slots

### Server Players Full Object (Server Players Simple Object)

  + `list`: `EPOCDOTFR`, `JACKMAYOL`, `PASIK` (array[string], required) - List of players usernames

### Server Simple Object

  + `name`: `InvasionEU2` (required) - Server name
  + `type_name`: `Vanilla` (required) - Server type name (mod used)
  + `mode_name`: `Coop.` (required) - Server mode name (game mode)
  + `is_ranked`: `true` (boolean, required) - Whether this server is an official one
  + `website_url`: `https://runningwithrifles.com/` - Server website URL
  + `url`: `https://rwrstats.com/servers/31.186.250.67:1235/invasioneu2` (required) - RWRS website URL
  + `map` (Server Map Simple Object) - Server map
  + `players` (Server Players Simple Object) - Server players
  + `location` (Server Location Object) - Server location

### Server Full Object (Server Simple Object)

  + `map` (Server Map Full Object) - Server map
  + `version`: `1.70.2` (required) - Server game version
  + `database_name`: `Invasion` (required) - Databse name
  + `mode_name_long`: `Cooperation` (required) - Long server mode name
  + `is_dedicated`: `true` (boolean, required) - Whether this server is a dedicated one
  + `comment`: `Coop campaign` - Server comment
  + `bots`: `104` (number, required) - Number of current bots
  + `players` (Server Players Full Object) - Server players

### Error Response

  + message: `Example error message` (required) - The error message explaining what happened