### Database Object

  + `id`: `invasion` (nullable) - Database ID. `null` if not a ranked server or not a ranked player profile
    + Default: `null`
  + `name`: `Invasion` (nullable) - Database name. `null` if not a ranked server or not a ranked player profile
    + Default: `null`

### Error Response

  + message: `Example error message` (required) - The error message explaining what happened